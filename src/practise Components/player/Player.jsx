import React, { use } from 'react';

const Player = ({playerPromise,coin,setCoin}) => {
    const PlayerPromise = use(playerPromise)
    return (
        <div>
            
        </div>
    );
};

export default Player;